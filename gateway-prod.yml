server:
  port: 8094

eureka:
  client:
    serviceUrl:
      defaultZone: # To be defined

spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
      - id: peliculas-actores
        uri: # To be defined
        predicates:
        - Path=/api/peliculas/**
        filters:
        - StripPrefix=2
      - id: usuarios-criticas
        uri: # To be defined
        predicates:
        - Path=/api/usuarios/**
        filters:
        - StripPrefix=2

springdoc:
  api-docs:
    path: /api/api-docs
