server:
  port: 8094

eureka:
  client:
    serviceUrl:
      defaultZone: http://:8762/eureka

spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
      - id: servicio-peliculas-actores
        uri: # To be defined
        predicates:
        - Path=/api/peliculas/**
        filters:
        - StripPrefix=2
      - id: servicio-usuarios-criticas
        uri: # To be defined
        predicates:
        - Path=/api/usuarios/**
        filters:
        - StripPrefix=2

springdoc:
  api-docs:
    path: /api/api-docs
